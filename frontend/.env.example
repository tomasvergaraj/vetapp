# URL base del backend. En desarrollo Vite tiene un proxy en /api
# que apunta a http://localhost:8000 (ver vite.config.js).
# En producción, define aquí el dominio del backend.
VITE_API_BASE_URL=/api
